import { createMuiTheme } from '@material-ui/core/styles';
export const theme = createMuiTheme({ 
    palette: { 
        primary: { main: '#f44336' }, 
        secondary: { main: '#20B2AA' }, 
        }, 
        status: { danger: 'orange', }, 
    }
);

//to jest miejsce gdzie definiuje się tematy (style) z jakich będzie korzystała nasza aplikacja
